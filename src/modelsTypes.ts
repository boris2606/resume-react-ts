export type Navigation = {
    name: string
    path: string
}

export type ButtonLinkType = {
    name: string
    path: string
}

export type Offers = {
    title: string
    descr: string
    img: string
}

export type SkillsType = {
    name: string
    descr: string
    img: string
    learning: boolean
    other: boolean
    lang: boolean
}